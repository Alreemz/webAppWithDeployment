import React, { useState } from 'react'; import axios from 'axios'; const AssignRole = () => { const [teamId, setTeamId] = useState(''); const [roleId, setRoleId] = useState(''); const handleAssign = () => { axios.post(`/api/assign/${teamId}/${roleId}`).then(alert('Role Assigned!')); }; return (<div><input placeholder='Team ID' value={teamId} onChange={e => setTeamId(e.target.value)}/><input placeholder='Role ID' value={roleId} onChange={e => setRoleId(e.target.value)}/><button onClick={handleAssign}>Assign Role</button></div>); }; export default AssignRole;