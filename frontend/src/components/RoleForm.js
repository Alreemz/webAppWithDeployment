import React, { useState } from 'react'; import axios from 'axios'; const RoleForm = () => { const [title, setTitle] = useState(''); const handleSubmit = (e) => { e.preventDefault(); axios.post('/api/roles', { title }).then(() => setTitle('')); }; return (<form onSubmit={handleSubmit}><input value={title} onChange={e => setTitle(e.target.value)} placeholder='Role Title'/><button type='submit'>Add Role</button></form>); }; export default RoleForm;