import React, { useState } from 'react'; import axios from 'axios'; const TeamForm = () => { const [name, setName] = useState(''); const handleSubmit = (e) => { e.preventDefault(); axios.post('/api/teams', { name }).then(() => setName('')); }; return (<form onSubmit={handleSubmit}><input value={name} onChange={e => setName(e.target.value)} placeholder='Team Name'/><button type='submit'>Add Team</button></form>); }; export default TeamForm;